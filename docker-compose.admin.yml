version: '3.8'
services:
  admin:
    build:
      context: .
      dockerfile: Dockerfile.admin
    ports:
      - "8080:8080"
    environment:
      - ADMIN_PORT=8080
      - ADMIN_SECRET=${ADMIN_SECRET:-changeme}
    volumes:
      - ./models:/app/models:rw
